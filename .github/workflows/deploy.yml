name: Deploy to TestFlight

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Copy signing keys
        run: |
          cp -r /Users/shloimyfuchs/Desktop/calorie\ tracker/private_keys ./private_keys

      - name: Deploy to TestFlight
        run: ./deploy.sh
